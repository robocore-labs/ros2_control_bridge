<library path="ros2_control_bridge">
  <class
    name="ros2_control_bridge/TopicBridge"
    type="ros2_control_bridge::TopicBridge"
    base_class_type="hardware_interface::SystemInterface">
    <description>
      Robot-agnostic ros2_control SystemInterface. Publishes controller commands
      as Float64MultiArray on one topic per joint group and reads joint states
      back from a JointState topic. Groups, topics and interfaces all come from
      the URDF; nothing about a particular robot is compiled in.
    </description>
  </class>
</library>
